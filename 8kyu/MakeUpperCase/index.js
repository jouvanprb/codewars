function makeUpperCase(str) {
    // Convert str function to Upper Case.
    let textUpperCase = str.toUpperCase();
    return textUpperCase;
}

console.log(makeUpperCase('Hello'));
console.log(makeUpperCase(''));
